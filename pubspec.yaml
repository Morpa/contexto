name: contexto
description: "A simple and easy to shared data on Flutter."
version: 1.0.0
homepage: "https://github.com/Morpa/contexto"

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
